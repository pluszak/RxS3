machine:
  java:
    version: oraclejdk8

dependencies:
  post:
    - ./bin/fakes3 -r . -p 12345:
        pwd:
          fake-s3/
        background: true


deployment:
  production:
    branch: master
    commands:
      - chmod u+x gradlew
      - ./gradlew release