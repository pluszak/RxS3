machine:
  java:
    version: oraclejdk8
  post:
    - ./fake-s3/bin/fakes3 -r /mnt/fakes3 -p 12345
      background: true


deployment:
  production:
    branch: master
    commands:
      - chmod u+x gradlew
      - ./gradlew release