machine:
  java:
    version: oraclejdk8

dependencies:
  post:
    - fakes3 -r /mnt/fakes3 -p 12345:
        pwd:
          fake-s3/bin/
        background: true


deployment:
  production:
    branch: master
    commands:
      - chmod u+x gradlew
      - ./gradlew release